services:
  - type: web
    name: whiskerwatch
    runtime: node
    buildCommand: npm run install:all && npm run build && npm run seed
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
